// Dependencies on clean linux
sudo apt install npm
sudo npm i -g electron-builder
sudo npm cache clean -f
sudo npm install -g n
sudo n 12.16.0

# remove node_modules and package-lock.json

npm install

//Build release
sudo npm run dist


// Run application
sudo npm start

App version needs updating in config.js and package.json

Adding plugin example
npm install ref-array

May need to run below after adding a plugin
./node_modules/.bin/electron-rebuild

//Change icon png permissions on files in build icons
sudo chmod 0644 *


//USB changes (need to build release)
npm install -g node-gyp


node_modules/usb/binding.gyp to include module_name and module_path:
  'variables': {
    'use_udev%': 1,
    'use_system_libusb%': 'false',
    'module_name': 'usb_bindings',
    'module_path': './src/binding'
  },

cd node_modules/usb
node-gyp rebuild --target=0.30.1 --dist-url=https://atom.io/download/atom-shell



//Build release
sudo npm run dist